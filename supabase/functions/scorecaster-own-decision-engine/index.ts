import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
const CHAMPION_ID="scorecaster-own-football-baseline",CHALLENGER_ID="scorecaster-own-football-ml",VERSION="scorecaster-own-decision-engine-v1.3",OUTCOMES=["home","draw","away"] as const,CLUB_TOKENS=new Set(["1","fc","afc","cf","sc","ac","fk","bk","if","aif","ud","cd","rc","rcd","ssc","club","football","calcio","futbol","de"]);
const finite=(v:unknown,fallback:number|null=null)=>{if(v===null||v===undefined||v==="")return fallback;const n=Number(v);return Number.isFinite(n)?n:fallback;},clamp=(v:number,lo=0,hi=1)=>Math.max(lo,Math.min(hi,v)),teamKey=(v:unknown)=>String(v||"").toLowerCase().normalize("NFKD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g," ").split(/\s+/).filter(t=>t&&!CLUB_TOKENS.has(t)).join("-");
function sameTeam(l:unknown,r:unknown){const a=teamKey(l),b=teamKey(r);if(!a||!b)return false;if(a===b)return true;const s=a.length<=b.length?a:b,g=a.length<=b.length?b:a;return s.length>=5&&g.startsWith(`${s}-`);}function bucket(date=new Date(),minutes=30){const d=new Date(date);d.setUTCMinutes(Math.floor(d.getUTCMinutes()/minutes)*minutes,0,0);return d.toISOString();}async function hash(value:unknown){const b=new TextEncoder().encode(JSON.stringify(value)),h=await crypto.subtle.digest("SHA-256",b);return[...new Uint8Array(h)].map(x=>x.toString(16).padStart(2,"0")).join("");}function validP(p:any){const vals=OUTCOMES.map(k=>finite(p?.[k]));return vals.every(v=>v!==null&&v!>=0&&v!<=1)&&Math.abs(vals.reduce((s,v)=>s+Number(v),0)-1)<.02;}function selection(p:any){return OUTCOMES.reduce((best,k)=>p[k]>p[best]?k:best,"home" as typeof OUTCOMES[number]);}function confidence(p:any){const s=OUTCOMES.map(k=>Number(p[k])).sort((a,b)=>b-a);return clamp(.65*s[0]+.35*(s[0]-s[1]));}function maxGap(l:any,r:any){return validP(l)&&validP(r)?Math.max(...OUTCOMES.map(k=>Math.abs(Number(l[k])-Number(r[k])))):null;}async function fetchRows(admin:any,table:string,select:string,configure:(q:any)=>any,max=3000){const rows:any[]=[];for(let from=0;from<max;from+=1000){let q=admin.from(table).select(select).range(from,from+999);q=configure(q);const{data,error}=await q;if(error)throw error;rows.push(...(data||[]));if(!data||data.length<1000)break;}return rows;}function latestBy(rows:any[],keyFn:(r:any)=>string){const map=new Map<string,any>();for(const row of rows){const key=keyFn(row);if(key&&!map.has(key))map.set(key,row);}return map;}
function buildLatestMarketQuotes(rows:any[]){const latest=new Map<string,string>();for(const row of rows)if(row?.event_id&&row?.captured_at&&!latest.has(row.event_id))latest.set(row.event_id,row.captured_at);const grouped=new Map<string,Map<string,any>>();for(const row of rows){if(!row?.event_id||row.market!=="h2h"||row.captured_at!==latest.get(row.event_id))continue;const name=String(row.selection||"").trim(),key=name.toLowerCase()==="draw"?"draw":teamKey(name);if(!key)continue;if(!grouped.has(row.event_id))grouped.set(row.event_id,new Map());const event=grouped.get(row.event_id)!;if(!event.has(key))event.set(key,{selection:name,prices:[],probabilities:[],sourceId:row.source_id||null});const q=event.get(key),p=finite(row.price),prob=finite(row.normalized_probability);if(p!==null&&p>1)q.prices.push(p);if(prob!==null&&prob>0&&prob<1)q.probabilities.push(prob);}const result=new Map<string,any[]>();for(const[eventId,event]of grouped.entries())result.set(eventId,[...event.values()].map((q:any)=>({selection:q.selection,sourceId:q.sourceId,bestOdds:q.prices.length?Math.max(...q.prices):null,consensusProbability:q.probabilities.length?q.probabilities.reduce((s:number,v:number)=>s+v,0)/q.probabilities.length:null,bookmakerCount:q.prices.length})));return result;}function findMarketQuote(m:Map<string,any[]>,sourceEventId:string,picked:string,fixture:any){const quotes=m.get(sourceEventId)||[];if(picked==="draw")return quotes.find(q=>String(q.selection||"").toLowerCase()==="draw")||null;const target=picked==="home"?fixture.home_team:fixture.away_team;return quotes.find(q=>sameTeam(q.selection,target))||null;}
Deno.serve(async req=>{if(req.method!=="POST")return Response.json({ok:false,error:"POST required"},{status:405});const url=Deno.env.get("SUPABASE_URL"),service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!url||!service)return Response.json({ok:false,error:"Supabase environment unavailable"},{status:503});const admin=createClient(url,service,{auth:{persistSession:false}}),presented=req.headers.get("x-scorecaster-cron-token")||"";const{data:auth}=await admin.from("scorecaster_internal_secrets_v1").select("secret_value").eq("name","own_decision_engine_cron").single();if(!presented||presented!==auth?.secret_value)return Response.json({ok:false,error:"Unauthorized"},{status:401});const now=new Date(),asOf=now.toISOString(),asOfBucket=bucket(now),futureEnd=new Date(now.getTime()+14*86400000).toISOString();try{const[fixtures,championRows,challengerRows,identityRows,artifactResult]=await Promise.all([fetchRows(admin,"scorecaster_event_outcomes_v1","event_id,sport_key,league,home_team,away_team,commence_time,status",q=>q.in("status",["scheduled","unknown"]).gte("commence_time",asOf).lte("commence_time",futureEnd).order("commence_time",{ascending:true}),5000),fetchRows(admin,"scorecaster_model_predictions_v1","event_id,as_of,model_id,model_version,probabilities,expected_scores,training_data_hash,prediction_hash,independent_from_market,shadow_only",q=>q.eq("model_id",CHAMPION_ID).order("as_of",{ascending:false}),3000),fetchRows(admin,"scorecaster_model_predictions_v1","event_id,as_of,model_id,model_version,probabilities,expected_scores,training_data_hash,prediction_hash,independent_from_market,shadow_only",q=>q.eq("model_id",CHALLENGER_ID).order("as_of",{ascending:false}),3000),fetchRows(admin,"scorecaster_event_identity_map_v1","canonical_event_id,source_id,source_event_id,match_confidence,verified,lineage,updated_at",q=>q.eq("verified",true).order("updated_at",{ascending:false}),3000),admin.from("scorecaster_model_artifacts_v1").select("model_version,promotion_gate,artifact_hash,trained_at").eq("model_id",CHALLENGER_ID).order("trained_at",{ascending:false}).limit(1).maybeSingle()]);if(artifactResult.error)throw artifactResult.error;const champions=latestBy(championRows,r=>r.event_id),challengers=latestBy(challengerRows,r=>r.event_id),identities=latestBy(identityRows,r=>r.canonical_event_id),sourceEventIds=[...new Set(identityRows.map(r=>r.source_event_id).filter(Boolean))],marketRows=sourceEventIds.length?await fetchRows(admin,"market_provider_snapshots_v2","event_id,market,selection,price,normalized_probability,captured_at,source_id,bookmaker_key",q=>q.in("event_id",sourceEventIds).eq("market","h2h").order("captured_at",{ascending:false}),12000):[],marketQuotes=buildLatestMarketQuotes(marketRows),challengerGate=artifactResult.data?.promotion_gate||{},challengerTrusted=challengerGate?.ownBaselineReviewCandidate===true,rows:any[]=[],samples:any[]=[];for(const fixture of fixtures){const champion=champions.get(fixture.event_id);if(!champion||!validP(champion.probabilities))continue;const challenger=challengers.get(fixture.event_id)||null,picked=selection(champion.probabilities),fairP=Number(champion.probabilities[picked]),fairOdds=fairP>0?1/fairP:null,conf=confidence(champion.probabilities),gap=challenger&&validP(challenger.probabilities)?maxGap(champion.probabilities,challenger.probabilities):null,reasons:string[]=["scorecaster-owned-baseline-ready","market-not-used-by-champion"];let decision="OWN_PREDICTION_READY";const sorted=OUTCOMES.map(k=>Number(champion.probabilities[k])).sort((a,b)=>b-a),margin=sorted[0]-sorted[1];if(fairP<.42||margin<.07){decision="OWN_PREDICTION_CAUTION";reasons.push("low-separation");}if(challenger){reasons.push(challengerTrusted?"challenger-review-candidate":"challenger-shadow-not-trusted-for-gating");if(challengerTrusted&&gap!==null&&gap>.12){decision="OWN_PREDICTION_CAUTION";reasons.push("trusted-challenger-disagreement");}}else reasons.push("challenger-unavailable");let marketMapped=false,marketSourceId=null,marketSourceEventId=null,marketProbability=null,marketOdds=null,paperEdge=null,paperEv=null;const identity=identities.get(fixture.event_id);if(identity){const quote=findMarketQuote(marketQuotes,identity.source_event_id,picked,fixture);if(quote&&quote.consensusProbability!==null&&quote.bestOdds!==null){marketMapped=true;marketSourceId=quote.sourceId||identity.source_id;marketSourceEventId=identity.source_event_id;marketProbability=quote.consensusProbability;marketOdds=quote.bestOdds;paperEdge=fairP-marketProbability;paperEv=fairP*marketOdds-1;reasons.push("full-market-price-mapped-for-paper-comparison");}else reasons.push("market-selection-quote-unavailable");}else reasons.push("market-event-unmapped");const decisionHash=await hash({eventId:fixture.event_id,asOfBucket,engineVersion:VERSION,championVersion:champion.model_version,challengerVersion:challenger?.model_version||null,championPredictionHash:champion.prediction_hash,marketSourceEventId,marketProbability,marketOdds}),row={decision_hash:decisionHash,event_id:fixture.event_id,sport_key:fixture.sport_key||"soccer",league:fixture.league,home_team:fixture.home_team,away_team:fixture.away_team,commence_time:fixture.commence_time,as_of:asOf,as_of_bucket:asOfBucket,intelligence_decision:decision,selected_outcome:picked,fair_probability:fairP,fair_odds:fairOdds,confidence_score:conf,champion_model_id:champion.model_id,champion_model_version:champion.model_version,champion_probabilities:champion.probabilities,challenger_model_id:challenger?.model_id||null,challenger_model_version:challenger?.model_version||null,challenger_probabilities:challenger?.probabilities||null,challenger_status:challenger?(challengerTrusted?"review-candidate":"shadow"):"unavailable",disagreement_gap:gap,market_mapped:marketMapped,market_source_id:marketSourceId,market_source_event_id:marketSourceEventId,market_probability:marketProbability,market_odds:marketOdds,paper_edge:paperEdge,paper_ev:paperEv,reason_codes:[...new Set(reasons)],provenance:{engineVersion:VERSION,championPredictionHash:champion.prediction_hash,championTrainingDataHash:champion.training_data_hash,challengerArtifactHash:artifactResult.data?.artifact_hash||null,eventIdentity:identity||null},production_play_upgrade_allowed:false,production_probability_changed:false,automatic_model_promotion_allowed:false,real_money_action_available:false,paper_only:true};rows.push(row);if(samples.length<8)samples.push({eventId:row.event_id,match:`${row.home_team} vs ${row.away_team}`,decision,selection:picked,fairProbability:fairP,fairOdds,confidence:conf,challengerGap:gap,marketMapped,paperEdge,paperEv});}for(let i=0;i<rows.length;i+=300){const{error}=await admin.from("scorecaster_own_decisions_v1").upsert(rows.slice(i,i+300),{onConflict:"event_id,as_of_bucket,champion_model_id,champion_model_version"});if(error)throw error;}await admin.from("scorecaster_source_health_snapshots_v1").insert({captured_at:asOf,source_id:"scorecaster_own_decision_engine",status:rows.length?"healthy":"degraded",last_observed_at:asOf,age_minutes:0,records_24h:rows.length,rights_ok:true,training_rights_ok:true,dependency_class:"primary",diagnostics:{version:VERSION,decisions:rows.length,ready:rows.filter(r=>r.intelligence_decision==="OWN_PREDICTION_READY").length,caution:rows.filter(r=>r.intelligence_decision==="OWN_PREDICTION_CAUTION").length,marketMapped:rows.filter(r=>r.market_mapped).length,championModelId:CHAMPION_ID,challengerTrusted,independentFromMarketChampion:true},paper_only:true});return Response.json({ok:true,version:VERSION,asOf,decisions:rows.length,ready:rows.filter(r=>r.intelligence_decision==="OWN_PREDICTION_READY").length,caution:rows.filter(r=>r.intelligence_decision==="OWN_PREDICTION_CAUTION").length,marketMapped:rows.filter(r=>r.market_mapped).length,champion:{id:CHAMPION_ID,role:"owned-data-baseline-champion"},challenger:{id:CHALLENGER_ID,trustedForGating:challengerTrusted,gate:challengerGate},samples,productionPlayUpgradeAllowed:false,productionProbabilityChanged:false,automaticModelPromotionAllowed:false,realMoneyActionAvailable:false,paperOnly:true},{headers:{"Cache-Control":"no-store"}});}catch(error){const detail=error&&typeof error==="object"?JSON.stringify(error):String(error);return Response.json({ok:false,version:VERSION,error:detail,paperOnly:true},{status:500});}});