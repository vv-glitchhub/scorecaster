import ModelLabClient from "./ModelLabClient";

export const metadata = {
  title: "Professional Model Lab | Scorecaster",
  description: "Simple and professional explanations, formula registry, reconciled contributions and reproducible event snapshots."
};

export default function ModelLabPage() {
  return <ModelLabClient />;
}
